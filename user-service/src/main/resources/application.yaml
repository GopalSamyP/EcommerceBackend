server:
  port: 8081

spring:
  application:
    name: USER-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/user_db
    username: root
    password: SecurePass@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
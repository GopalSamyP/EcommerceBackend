server:
  port: 8082

spring:
  application:
    name: ORDER-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/order_db
    username: root
    password: SecurePass@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true